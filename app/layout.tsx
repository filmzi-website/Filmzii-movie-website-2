import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import "./globals.css"

export const metadata: Metadata = {
  title: "Filmzi - Stream & Download Movies and TV Series",
  description:
    "Watch and download the latest movies and TV series in HD quality. Free streaming platform with 720p and 1080p options.",
  generator: "Filmzi",
  keywords: "movies, tv series, streaming, download, HD, 720p, 1080p, free",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <head>
        <link rel="icon" href="https://envs.sh/uiU.jpg" />
        <style>{`
html {
  font-family: ${GeistSans.style.fontFamily};
  --font-sans: ${GeistSans.variable};
  --font-mono: ${GeistMono.variable};
}
        `}</style>
      </head>
      <body className="dark">{children}</body>
    </html>
  )
}
